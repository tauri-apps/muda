---
"muda": minor
---

Breaking change: update the public `Error` enum for GTK initialization failures, including renamed/changed variants, and add GTK-specific errors for windows without an application and unsupported containers.
