---
"muda": minor
---

Add native icon support on exsiting `NativeIcon` variants on GTK and Windows where possible. Also added `NativeIcon::Raw`, `NativeIcon::from_name` on macOS/Linux/BSD, or `NativeIcon::from_id` on Windows for using raw platform specific values if needed.
