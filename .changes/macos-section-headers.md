---
"muda": minor
---

Add `PredefinedMenuItem::section_header` for native menu section headers on macOS 14 and later. Section headers are rendered as disabled menu items on unsupported macOS versions and other platforms.
