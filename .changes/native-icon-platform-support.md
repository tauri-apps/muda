---
"muda": minor
---

Add native icon string support on GTK and Windows. Native icon strings use AppKit image names on macOS, exact `SIID_*` stock icon names or IDs on Windows, and icon theme names on GTK.
